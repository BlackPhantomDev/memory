/**
 * Application entry point.
 *
 * Loads the styles and the dev theme switcher, then boots the screen flow.
 *
 * @module main
 */

import './styles/main.scss';
import { initApp } from './js/screens';

initApp();
